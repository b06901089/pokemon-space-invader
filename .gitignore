### To clean the repo, run the following commands in terminal:
# git rm -r --cached . 
# (git rm -r --cached <file_or_directory>)
# git add .
# git commit -m "Clean repo using .gitignore"
# git push

img/org_img
img/pokemon
img/Red Envelope Starter Pack

__pycache__/
*.pyc